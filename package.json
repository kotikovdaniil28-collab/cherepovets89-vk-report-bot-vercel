{
  "name": "cherepovets89-vk-report-bot-vercel-v4-ai-rules",
  "version": "4.0.0",
  "private": true,
  "description": "VK Callback API bot for CH89 reports, staff routing, concise DeepSeek AI with BLACK RUSSIA moderation rules, owner commands, and table notifications.",
  "scripts": {
    "dev": "npx vercel dev",
    "deploy": "npx vercel --prod",
    "check": "node --check api/vk.js && node --check api/table-webhook.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4"
  }
}
