{
  "name": "cherepovets89-vk-report-bot-vercel-xai",
  "version": "38.0.0",
  "private": true,
  "description": "VK Callback API bot for CHEREPOVETS reports, moderation, Google Sheets, staff workflows, xAI/Grok chat, Grok Vision, Grok Imagine, per-user AI memory, welcome messages and group rules.",
  "scripts": {
    "dev": "npx vercel dev",
    "deploy": "npx vercel --prod",
    "check": "node --check api/vk.js && node --check api/table-webhook.js && node --check api/google-sheet-webhook.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4"
  }
}
