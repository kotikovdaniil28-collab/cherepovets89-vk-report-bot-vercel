{
  "name": "cherepovets89-vk-report-bot-vercel-xai",
  "version": "50.0.0",
  "private": true,
  "description": "CHEREPOVETS VK bot with silent report review callbacks, moderator career ladder, Google Sheets roster sync and staff workflows.",
  "scripts": {
    "dev": "npx vercel dev",
    "deploy": "npx vercel --prod",
    "check": "node --check api/vk.js && node --check api/table-webhook.js && node --check api/google-sheet-webhook.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4"
  }
}
