{
  "name": "cherepovets89-vk-report-bot-vercel-v4-ai-rules",
  "version": "30.0.0",
  "private": true,
  "description": "VK Callback API bot for CHEREPOVETS reports, moderation, Google Sheets, staff workflows, Gemini/DeepSeek AI chat, per-user AI memory, image generation, welcome messages, group rules, fixed VK buttons and safe Supabase startup.",
  "scripts": {
    "dev": "npx vercel dev",
    "deploy": "npx vercel --prod",
    "check": "node --check api/vk.js && node --check api/table-webhook.js && node --check api/google-sheet-webhook.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4"
  }
}
